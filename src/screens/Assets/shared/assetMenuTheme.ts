import { AppColors, C } from "../../../utils/helpers/colors";
import { elevation } from "../../../utils/helpers/tokens";

export const ASSET_MENU_BRAND_RED = C.red;

export const assetMenuCardShadow = (c: AppColors) => elevation(c.shadow, 1);
