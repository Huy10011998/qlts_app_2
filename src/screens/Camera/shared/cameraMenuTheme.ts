import { AppColors, C } from "../../../utils/helpers/colors";
import { elevation } from "../../../utils/helpers/tokens";

export const CAMERA_MENU_BRAND_RED = C.red;

export const cameraMenuCardShadow = (c: AppColors) => elevation(c.shadow, 1);
