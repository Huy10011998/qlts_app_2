// Enum cho TypeGroup của Item
export enum TypeGroup {
  ROOT = 0,
  CATEGORY = 1,
  ITEM = 2,
}

// Enum cho TypeProperty của Field
export enum PropertyType {
  STRING = 1,
  NUMBER = 2,
  DATE = 3,
  BOOLEAN = 4,
}
